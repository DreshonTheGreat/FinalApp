@page
@model FirstProjectApp_SANDERS.Pages.WeatherAppModel
@{
}

<form method="post">
<div class="container">
    <div class="row">
        <div class="col text-center"><input type="submit" value="Click to get Forecast" /></div>
    </div>
</div>
</form>

@if(Model != null)
{
    
    <div class="container">
    <div class="row">
        <div class="col text-center">@Model.ForecastResponseBody</div>
    </div>
    <div class="row">
        <div class="col text-center">@Model.insightOneResults</div>
    </div>
    <div class="row"> 
        <div class="col text-center">@Model.insightTwoResults</div>
    </div>
</div>
}
